/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['ibb.co', 'i.ibb.co'], // Adding both 'ibb.co' and 'i.ibb.co' to the domain list
    },
  };
  
  export default nextConfig;
  
  
